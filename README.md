# Vaeroex Ops System

Vaeroex Ops System is a SaaS-style operations platform for small and mid-sized businesses. It helps teams turn messy workflows into trackable systems: business intake, operations audits, forms, checklists, SOPs, tasks, issue logs, asset tracking, workflow maps, follow-up systems, reports, and Vaeroex-generated recommendations.

The in-app operations partner is named **Vaeroex**. User-facing UI should say things like "Ask Vaeroex", "Vaeroex Hub", "Run Vaeroex Audit", and "Generated by Vaeroex". Do not use generic names in product UI.

## Tech Stack

- Next.js App Router
- TypeScript
- Tailwind CSS
- Supabase Auth
- Supabase Postgres
- Supabase Storage-ready schema
- Supabase Row Level Security
- OpenAI API for Vaeroex features
- Vercel-ready deployment

## Current Phase

Phase 1 is complete in this scaffold:

- Project structure
- `AGENTS.md`
- Main Vaeroex system prompt
- Supabase migration with schema and RLS policies
- Demo seed data
- Industry template data
- Environment example
- README draft

Phase 2 is now in progress/completed in the scaffold:

- Supabase auth actions
- Login, signup, forgot password, and accept-invite pages
- Supabase email callback route
- Protected `/app` layout
- Sidebar navigation, mobile navigation, topbar, workspace switcher, user profile area, and sign out
- Workspace owner membership creation
- Role-aware workspace context
- Setup wizard
- Industry template selection
- Post-setup workspace generation with starter forms, checklists, workflows, SOP drafts, issue categories, tasks, assets, report, and Vaeroex audit result

Phase 3 is now implemented in the scaffold:

- Live dashboard metrics, recent tasks, recent issues, and latest Vaeroex summary/report surfaces
- Forms module with form creation, form detail, submissions, and submission-to-task conversion
- Checklists module with checklist creation and checklist run history
- Tasks module with creation, status updates, priority views, and Vaeroex-generated labeling
- Issues module with issue logging, root cause/fix capture, and issue-to-task conversion
- Assets module with asset register, readiness checks, and check history
- People module with operational directory records
- SOP module with draft creation, version/status display, and Vaeroex-generated labeling
- Reports module with report creation, workspace metrics, source data preview, and Generated by Vaeroex labeling
- Shared loading, empty, error, status, form, card, JSON preview, and page header components

Phase 4 is now implemented in the scaffold:

- Vaeroex Hub execution UI
- Ask Vaeroex general chat
- Operations Audit, SOP Generator, Weekly Report, Daily Summary, Bottleneck Detector, Form Builder, Checklist Builder, and Follow-Up workflows
- Shared Vaeroex OpenAI wrapper using `VAEROEX_SYSTEM_PROMPT` as the base prompt for every run
- Workspace context snapshot for tenant-safe Vaeroex runs
- Every Vaeroex run saved to `ai_agent_runs`, including failed runs when possible
- Confirmation-only save actions for tasks, SOPs, forms, checklists, and reports

Phase 5 is now implemented in the scaffold:

- Enriched demo seed data across all core modules
- App-wide compliance notice plus stronger setup and Vaeroex notices
- Toast notifications from server-action redirects
- Confirmation prompts before workspace generation, Vaeroex saves, and submission/issue task conversion
- Route-level loading skeletons for operations modules
- App and route error boundaries
- Broader auth and operations validation
- Vercel deployment instructions
- Acceptance review in `docs/phase-5-acceptance.md`

Phase 6 is now implemented in the scaffold:

- Squarespace-based subscription access model
- Subscription plans, customer subscriptions, subscription events, manual activation requests, and Vaeroex usage tracking
- Billing-required page with manual activation request flow
- Account subscription page with status, plan, usage, upgrade, and contact links
- Owner/admin subscription management page for manual activations, status changes, notes, requests, and event payload review
- Squarespace webhook endpoint that stores order events and maps product names/SKUs to the Vaeroex plan
- Plan limits for workspaces, users, forms, checklists, and monthly Vaeroex runs
- Pricing page buttons that link to Squarespace checkout URLs
- Squarespace setup docs and thank-you page copy
- Acceptance review in `docs/phase-6-acceptance.md`

Phase 7 admin and support tools are now implemented in the scaffold:

- Internal Vaeroex admin area at `/app/admin`
- Customer, workspace, subscription, Vaeroex usage, support request, and audit log admin pages
- Internal admin access controlled by `VAEROEX_ADMIN_EMAILS`
- Manual workspace access activation/deactivation tools
- Support request forms at `/support` and `/app/support`
- `support_requests` table with RLS, status, priority, and workspace/user links
- Impersonation placeholder only; customer impersonation is not implemented
- Acceptance review in `docs/phase-7-admin-support-acceptance.md`

Final pre-launch documentation is available:

- `docs/pre-launch-checklist.md`
- `docs/manual-testing-script.md`
- `docs/known-limitations.md`
- `docs/version-1-roadmap.md`

## Local Setup

1. Install dependencies:

```bash
pnpm install
```

2. Copy environment variables:

```bash
cp .env.example .env.local
```

3. Fill in Supabase and OpenAI values in `.env.local`.

4. Start the dev server:

```bash
pnpm dev
```

Use polling mode if local file watching is unreliable:

```bash
pnpm dev:poll
```

5. Run validation:

```bash
pnpm typecheck
pnpm build
```

## Environment Variables

```bash
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
OPENAI_API_KEY=
OPENAI_MODEL=
CRON_SECRET=
RESEND_API_KEY=
NEXT_PUBLIC_APP_URL=
VAEROEX_ADMIN_EMAILS=
SQUARESPACE_API_KEY=
SQUARESPACE_WEBHOOK_SECRET=
SQUARESPACE_VAEROEX_PRODUCT_ID=
SQUARESPACE_VAEROEX_SKU=
NEXT_PUBLIC_SQUARESPACE_VAEROEX_CHECKOUT_URL=
```

`SUPABASE_SERVICE_ROLE_KEY` must only be used in server-side code. Never expose it to the browser.
`CRON_SECRET` protects scheduled report subscription jobs. Add the same secret to Vercel and keep it server-side only.

`VAEROEX_ADMIN_EMAILS` is a comma-separated allowlist for internal Vaeroex owner/admin tools, for example:

```bash
VAEROEX_ADMIN_EMAILS=owner@vaeroex.com,admin@vaeroex.com
```

## Selling Through Squarespace

Vaeroex Ops System does not run its own checkout flow yet. Squarespace handles the storefront, checkout, payment collection, taxes, customer purchase flow, and subscription billing.

The Vaeroex app handles account login, workspace creation, product access, usage limits, and subscription status checks. Customers should create their Vaeroex app account with the same email address they used during Squarespace checkout so the app can match their purchase to `customer_subscriptions`.

For early customers, confirm the purchase in Squarespace and use `/app/admin/subscriptions` to manually activate the customer email, assign the Vaeroex plan, set the status, and add notes. Manual activations unlock app access without requiring webhook credentials.

For automated access, configure Squarespace order webhooks to send events to `/api/squarespace/webhook`. The app stores incoming payloads in `subscription_events`, maps known Squarespace product names, product IDs, or SKUs to the single internal `vaeroex` plan, and creates or updates `customer_subscriptions` when the payload is clear. Unclear payloads are retained for manual review.

The app remains usable when Squarespace webhook credentials are not configured. Customers without active, demo, or manually unlocked access are sent to `/billing-required`.

## Internal Admin And Support

The internal admin area starts at `/app/admin` and is intended for Vaeroex owner/admin use. It requires an authenticated user whose email is listed in `VAEROEX_ADMIN_EMAILS`, plus `SUPABASE_SERVICE_ROLE_KEY` for server-side admin reads and updates.

Admin tools include:

- `/app/admin/customers`
- `/app/admin/workspaces`
- `/app/admin/subscriptions`
- `/app/admin/ai-usage`
- `/app/admin/support-requests`
- `/app/admin/audit-logs`

Internal admins can search customers by email, view workspace and subscription status, manually activate or deactivate workspace/subscription access, review Vaeroex usage, inspect recent errors, review support requests, and inspect audit logs.

Impersonation is intentionally a placeholder only. Do not enable customer impersonation until it has a complete audit trail, approval flow, and session boundary.

Customers can submit support requests from `/support` or `/app/support`. The request stores name, email, workspace reference, issue type, message, priority, status, user ID when available, and workspace ID when available. Screenshot/file upload is represented as a placeholder and does not store files yet.

## Supabase Setup

1. Create a Supabase project.
2. Connect the project with the Supabase CLI.
3. Run migrations:

```bash
supabase db push
```

4. Seed demo data:

```bash
supabase db reset
```

The initial migration is:

```text
supabase/migrations/202606170001_phase_1_schema_rls.sql
```

The demo seed file is:

```text
supabase/seed.sql
```

## Database and RLS

All tenant-scoped operational records include `workspace_id`. RLS helper functions control access:

- `is_workspace_member(workspace_id)`
- `workspace_member_role(workspace_id)`
- `has_workspace_role(workspace_id, roles)`
- `can_manage_workspace(workspace_id)`
- `can_edit_operations(workspace_id)`

Role behavior:

- Owners: full workspace access, member management, settings, delete workspace
- Admins: full workspace management except delete workspace
- Managers: create and edit operations records and run Vaeroex reviews
- Staff: submit forms, complete assigned tasks, complete checklist runs, add asset checks
- Viewers: read-only dashboard and reports

Public form submissions are allowed only when the target form is public. The app should look up public forms by `public_slug` before inserting a submission.

## Vaeroex Prompt

The main prompt lives in:

```text
lib/ai/prompts/vaeroex-system-prompt.ts
```

It exports:

```ts
VAEROEX_SYSTEM_PROMPT
```

Every Vaeroex interaction must use that prompt. The app must not run a Vaeroex review without a non-empty system prompt.

Vaeroex runs use `OPENAI_API_KEY` and optionally `OPENAI_MODEL`. If `OPENAI_MODEL` is blank, the app uses the default configured in `lib/ai/vaeroex-client.ts`.

## Seed Data

Demo workspaces:

- Pacific Field Services
- Harbor Construction Co.
- Metro EMS Ops - Non Patient
- Elite Auto Detail
- Summit Fitness Studio

Each seeded workspace includes:

- 5 tasks
- 5 issues
- 3 forms
- 3 form submissions
- 3 checklists
- 4 checklist runs
- 3 SOPs
- 5 assets
- 2 asset checks
- 5 people
- 1 weekly report
- 4 Vaeroex results
- Demo subscription access on the Vaeroex plan
- 1 support request

## Demo Credentials

The seed creates a demo profile record and demo workspace memberships:

```text
demo@vaeroex.local
```

Supabase Auth users are separate from seeded profile rows. For a hosted demo, create a Supabase Auth user with this email or use the setup wizard to create a real account and workspace.

## Compliance Notice

Add this notice anywhere users enter operational information, especially setup, EMS templates, medical admin templates, settings, and Ask Vaeroex:

> Do not enter patient data, PHI, ePHI, Social Security numbers, medical record numbers, insurance IDs, or other regulated sensitive information unless your organization has the proper legal, compliance, security, and agreement requirements in place.

## What Is Complete

- Vercel-ready Next.js scaffold
- Tailwind configuration
- Agent instructions for future Codex work
- Main Vaeroex prompt file
- Supabase schema for workspaces, members, intake, workflows, forms, submissions, checklists, checklist runs, tasks, issues, assets, asset checks, people, SOPs, reports, Vaeroex runs, notifications, and audit logs
- RLS policies for tenant separation and role-aware access
- Demo seed data
- Industry starter template data
- Phase 3 dashboard and operations module MVP screens
- Tenant-scoped data access for dashboard, forms, submissions, checklists, checklist runs, tasks, issues, assets, asset checks, people, SOPs, and reports
- Phase 4 Vaeroex Hub, OpenAI wrapper, workspace snapshot, run storage, and confirmation save flow
- Phase 5 polish pass with demo data, loading/error/empty states, toast notifications, confirmations, validation, compliance notices, acceptance review, and Vercel instructions
- Phase 6 Squarespace subscription access with manual activation, webhook scaffolding, usage limits, account subscription status, admin review tools, pricing links, and setup docs
- Phase 7 internal admin and support tools with customer search, workspace access controls, subscription controls, Vaeroex usage review, support queue, audit log review, and support request forms

## What Is Placeholder

- Intake and workflow editing screens
- Supabase client helpers
- Storage upload implementation
- Email invites
- Stripe and internal checkout are intentionally not implemented; Squarespace is the subscription billing layer
- Integrations
- Edit/delete flows and richer filters
- Vaeroex result editing before confirmed save
- Live Squarespace webhook payloads still need to be verified against production Squarespace events
- Impersonation is a placeholder only
- Screenshot/file upload for support requests is a placeholder only
- Automated tests

## Next Development Tasks

Phase 2:

- Add richer member management UI on `/app/settings`
- Add invitation activation flow that links existing invited `workspace_members` rows to accepted users
- Add setup progress saving before final generation
- Add automated tests around workspace generation

Phase 3:

- Add edit/delete flows for operations modules
- Add richer filters and saved views
- Add confirmation modals and toast notifications

Phase 4:

- Add streaming responses for Ask Vaeroex
- Add richer prompt templates and output validation
- Add edit-before-save draft review screens
- Add workflow-specific result views

Phase 5:

- Add automated tests
- Run full browser acceptance tests after dependencies are installed
- Add edit/delete flows and richer filters
- Add Vaeroex result editing before confirmed save

## Vercel Deployment

Vercel supports Next.js projects and automatically uses the framework build settings. For Next.js, Vercel checks `package.json` for the `build` script and uses it as the Build Command; otherwise it falls back to `next build`. See the official Vercel docs for [Next.js](https://vercel.com/docs/frameworks/full-stack/nextjs), [build configuration](https://vercel.com/docs/builds/configure-a-build), and [environment variables](https://vercel.com/docs/environment-variables).

Before publishing or merging a deployment branch, run:

```bash
pnpm typecheck
pnpm build
pnpm security:check
git diff --check
```

The security check verifies core workspace isolation, admin-route guardrails, RLS migration coverage, private file storage policy markers, and server-secret client exposure patterns. Keep `/docs/security/*` updated whenever a route, table, storage bucket, or admin capability is added.

1. Push this project to GitHub, GitLab, or Bitbucket.
2. Create a Vercel project from the repository.
3. Use these project settings:

```text
Framework Preset: Next.js
Install Command: pnpm install
Build Command: pnpm build
Output Directory: .next
Node.js Version: Project default or latest Vercel-supported LTS
```

4. Add Vercel environment variables for Production and Preview:

```bash
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
OPENAI_API_KEY=
OPENAI_MODEL=
CRON_SECRET=
RESEND_API_KEY=
NEXT_PUBLIC_APP_URL=https://your-domain.example
VAEROEX_ADMIN_EMAILS=
```

5. In Supabase, add the deployed URL to auth redirect URLs:

```text
https://your-domain.example/auth/callback
```

6. Apply Supabase migrations before production use:

```bash
supabase db push
```

For a disposable demo database, load the demo seed with:

```bash
supabase db reset
```

7. Deploy from Vercel. After deployment, test signup, setup, dashboard, operations modules, Ask Vaeroex, and confirmed saves from Vaeroex results.

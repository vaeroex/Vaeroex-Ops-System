# Square production-readiness checklist

Audit baseline: `b4f08dc7f5f0f6e0ae61a03ff3bb139473e2a7fa` (PR #349 merged). This milestone adds **dormant ingestion and completeness policy with injected synthetic dependencies**. API `2026-08-19`, SDK `45.1.0` at `e4a5bf7e1a2b97c2b995fde28c55ddbc35dc0e76`, existing fingerprints and shared limits remain unchanged. No new phase identifier is assigned.

## Evidence levels

- **Implemented** means executable code exists for the stated boundary, not merely a descriptor declaration.
- **Fixture-tested** means deterministic synthetic/local coverage. The delivery PR's exact-head CI is the validation record.
- **End-to-end tested** requires a real, authorized isolated Square sandbox flow through verified authority, runtime and persistence. **No Square E2E evidence exists here.** Neither this synthetic adapter flow nor historical synthetic GCP verification is Square E2E.
- **Activated** requires separately authorized registry, database, credential, runtime and operational gates. **Square is not activated or registered.** QBO source, bindings, fingerprints and disabled-gate implementations are unchanged; fixture results do not attest to current live environment/database settings.

The [descriptor](../../lib/integrations/providers/square/descriptor.ts) still declares streams, backfill, concurrency two and a 500 ms default delay. These are not deployed synchronization or measured provider readiness. Its historical unsupported labels are preserved fingerprinted declarations, not an up-to-date inventory of dormant modules.

## Current capabilities and remaining requirements

All Square rows remain **not provider-E2E-tested / not activated**. Implementation here does not authorize the next column.

| Requirement | Implemented and synthetic evidence | Missing dependencies and acceptance test |
| --- | --- | --- |
| Trusted seller/location/tenant authority | [Adapter](../../lib/integrations/providers/square/ingestion-adapter.ts) requires an injected trusted resolver; binds workspace, Business Entity, connection, seller, environment, locations, operation/query and generation; rechecks before commit. Merchant/Location shape projections never confer authority. | OAuth-verified seller, location/default discovery, explicit Business Entity mapping and checked revocation/generation fencing. Foreign scope and revocation during a read must deny commits. ListPayments without a selector requires an explicit trusted default; it is not guessed. |
| Orders and Tenders | Four unchanged parsers plus separate core/line-item/adjustment/Tender pending-source scopes. Combined tests cover Retrieve/Batch/Search for each. | Durable source storage and separately authorized economic/overlap policy. Replay must preserve one content version; modifications/cancellation/late observations stay immutable, and richer scopes/Tender references cannot duplicate economic effects. |
| Payments and Refunds | Unchanged List/Get parsers plus pending mapping; exact Money, independent fees, nullable/unlinked semantics and unresolved references preserved. | Durable observations, eventual-consistency qualification and explicit reconciliation. Multiple/partial payments/refunds remain distinct; equal-clock conflict and late updates never silently replace newer state. Refunded aggregates are not Refund or settlement authority. |
| Catalog | Trusted four-operation facade, seven MVP types, explicit tombstones, separate primary/related/included observation roles. Existing [contract audit](square-catalog-response-contract.md) and bounds still apply. | Durable version retention and supported-variant qualification. Missing objects are never deletion; nested/related objects and applicability cannot resolve references or grant location authority. |
| Inventory | Five-operation parser and mapping for snapshots, physical counts and adjustments. [Pinned contract](square-inventory-response-contract.md) unchanged, including **unsigned cost_money**. | Durable optional-stream handling and real supported-state/consistency qualification. Exact quantity strings survive; UNTRACKED remains a provider observation, SUPPORTED_BY_NEWER_VERSION blocks the page, and no stock/valuation inference is made. |
| Returns, unsupported data and completeness | [Explicit policy](square-dormant-ingestion-contract.md): historical unknown, economic blocked, Orders always returns unknown. Unsupported/identity-less mixed pages cannot advance. | Versioned historical/backfill/overlap policy and later economic authority work. Return-bearing Orders, omitted fields, interrupted pages and finished cursors must never imply complete history, Current freshness or economic truth. No OrderReturn parser added. |
| Immutable pending versions | [Mapper](../../lib/integrations/providers/square/ingestion-mapping.ts) reuses generic source schemas, exact hashing and serializers. Stable resource identity differs from content and observation identity. Missing/equal revisions remain unordered/conflicted; late values do not replace current state. | Checked durable source/page transaction, retention and separately reviewed validation. Replay preserves first observation; changed content adds immutable versions; conflict/out-of-order/tombstones retain history. Current registry/SQL still reject Square. |
| Read client and errors | [Bounded client](../../lib/integrations/providers/square/ingestion-client.ts): injected synthetic transport only, existing policies, streamed byte limits, exact bounded decoder, redirect/destination denial, deadline/cancellation/cleanup and fixed safe codes. | Separately authorized live transport/credential integration, provider qualification and measured rate policy. Tests cover 16/64 MiB, precision/hostile JSON, 401/403/429/5xx, Retry-After, slow I/O/cancellation and sensitive canaries. |
| Page planning, restart and retries | [Atomic injected interface/model](../../lib/integrations/providers/square/ingestion-page-repository.ts): private query-bound cursors, deduplication, lease/generation/CAS, bounded retries and all-or-nothing checkpoint advancement. One page per invocation. | Durable transaction/uniqueness, protected cursor retention, expiry/restart/backfill policy, distributed rate coordination, scheduling and freshness thresholds. Before/during/after-commit faults, lost acknowledgements, swaps, stale generations and capacity must not skip uncommitted records. Memory publication is not database crash durability or distributed exactly-once. |
| OAuth and credentials | Generic broker, KMS/AAD, single-use state, refresh/revocation/CAS and historical synthetic GCP evidence remain unchanged. | Square endpoints/scopes/lifetimes/merchant verification, callback/refresh/revoke adapters, isolated secrets/IAM and checked credential authorization. Single-use state, foreign scope and concurrency races must fail safely with no credential canaries in logs or source projections. |
| Registration and database | [Registered registry](../../lib/integrations/control-plane/registered-provider-registry.ts) and SQL still authorize synthetic/QBO, not Square. No migrations, direct DML or service-role shortcuts. | Separately reviewed additive registration/source contracts, scoped mapping and atomic source/page RPCs; clean-install/upgrade/rollback and tenant/role tests. Square remains rejected until authorization, and all QBO behavior must remain unchanged. |
| Private runtime/scheduling/freshness | Existing generic task/checkpoint/CAS, Cloud Tasks/OIDC, lifecycle and freshness structures remain unchanged. Adapter returns plans only. | Deployment/IAM/queues, worker credentials, distributed rate fairness, overlap windows and thresholds. Restart/revocation/outage tests must recover boundedly; incomplete scans never become Current. No worker loop deployed here. |
| UI, sandbox E2E and activation | No Square UI, actual calls, webhook service, sandbox harness or activation. Automatic Vercel Preview verifies an application build only. | Role/CSRF-safe mapping/confirmation UI; explicitly authorized isolated app/seller and synthetic provider records; authority/database/runtime prerequisites, cleanup, payload-free observability/runbooks, kill switch and reviewed canary. Fresh OAuth through all required streams, immutable sources and restart/revocation must pass before activation, never using production credentials. |

## Reuse boundaries

Reuse unchanged generic exact/source/fact contracts and serializers; Square request/response policies; generic read-only POST policy; credential/state/KMS interfaces; task/lease/CAS concepts; lifecycle/freshness and safe status components. The new page contract requires one atomic transaction for source versions and checkpoint advancement; composing separate existing RPCs is not equivalent.

Do not reuse QBO-specific OAuth/realm verification, transport, revenue mapping, services, deployment bindings or credentials as generic infrastructure. [Provider validation repository](../../lib/integrations/persistence/provider-validation-repository.ts) still contains QBO-specific RPCs despite its generic name. TypeScript pending envelopes and the synthetic repository do not grant SQL/registry authority. The new mapper is not a canonical economic contribution mapper.

## Recommended next grouped milestone

**Square trusted-authority and durable-page persistence qualification, with activation still gated.** Replace synthetic authority and page-store assumptions with reviewed Square connection/mapping/generation contracts and an additive checked, atomic pending-source/page persistence boundary. Keep live execution, scheduling, UI and economic reconciliation separately gated.

Dependencies: independent review of this adapter's grant/identity/completeness/atomic contracts; preserved parser/request/descriptor goldens; explicit authorization for Square OAuth/credential and additive database work; approved cursor/source retention and revocation transaction semantics. Real provider qualification additionally requires a separately authorized isolated Square sandbox app/seller and synthetic records—never production credentials.

Acceptance tests:

- Verified seller/default-location discovery and explicit Business Entity mapping bind every grant; foreign scope and revocation/generation races deny reads and commits.
- Checked database authorization stays closed until reviewed registration; clean install, upgrade, rollback, tenant/role isolation and unchanged QBO fixtures pass.
- Source deduplication, immutable ordinals, revision conflicts/tombstones and private cursor/CAS advance in one durable transaction. Real connection/process interruption and lost acknowledgement recover without skips or duplicate versions.
- Cursor expiry, overlapping-window restart, unsupported/return limitations, retention/capacity and outages preserve incomplete state and bounded retries. Pagination alone never implies history-complete, Current or economic authority.
- Secret/cursor/provider canaries stay absent from logs; source envelopes remain pending/untrusted and canonical contributions blocked.

Private bounded runtime orchestration and explicitly authorized sandbox E2E remain necessary after that qualification, before UI/observability/canary activation. This recommendation is evidence for the next prompt, not authorization to begin it.

## This delivery's boundary

Implemented and fixture-tested: dormant client/decoder, explicit injected grants, eight pending projection scopes, completeness policy, resumable atomic page interface/model and all 25 operation/projection combinations in the synthetic integrated flow. See the [detailed contract and bound derivation](square-dormant-ingestion-contract.md).

No actual Square calls, OAuth implementation, real credentials, registry activation, database migrations or production persistence, runtime provisioning, queues/scheduler deployment, webhooks, UI, economic reconciliation, production activation, QBO changes, force-pushes or manual deployment. The original dirty QBO worktree remains outside this branch. The delivery PR is left open and unmerged for independent review.

# Square sandbox setup and approval worksheet

This is an unresolved-choice worksheet, **not authorization to execute**. No real values or secrets have been populated. Complete it through an approved private configuration/secret channel after independent review. Do not paste secrets in chat, source, PRs or ordinary database fields.

## Identity, host and mapping

- Confirm the isolated Square **Sandbox application ID** and seller/merchant ID; attest that neither is Production.
- Approve the exact HTTPS callback URI, clean return path `/app/settings/integrations/square`, sandbox test host and revocation-notification URL. The implemented code forbids redirect/query/environment substitutions. Approve a separate remote host/transport binding; current route enablement is loopback-only and refuses Vercel/Production.
- Specify the workspace and intended Business Entity UUID. Confirm the exact authenticated location IDs to map after discovery; confirm whether the authenticated default is included. Labels alone are not identity proof. Reauthorization may not replace the seller or entity.
- Identify the authorized initiating user/session and the separate broker, verified-enroller, runtime and webhook LOGINs. Approve exact capability memberships; ordinary customer/service roles must remain unable to manufacture enrollment.

## Credential access and exact uses

- Approve read access to one specific version of the sandbox application secret for code exchange and refresh. Confirm its application/environment binding. Local customer disconnect needs no secret access. Any later merchant/application revocation requires separate grant-level authority and exact target/impact approval; it is not available through these customer interfaces.
- Approve one sandbox KMS key and exact encrypt/decrypt permissions for the existing small OAuth credential envelope/AAD contract. No generic source-payload or QBO key access is needed.
- Approve access to the sandbox webhook signature key for HMAC verification of the exact notification URL plus raw body. Approve receiver registration separately.
- Choose the authorized browser operator for Square consent. OAuth access/refresh tokens are obtained only through the broker and retained only encrypted; no manual token copying into forms or fixtures.

## Retention, revocation and capacity

- Approve a finite, versioned source-retention duration, policy fingerprint and approval-expiry timestamp. No default duration is installed.
- Approve a private cursor lifetime, within the existing one-hour maximum, for all 4,096-character opaque cursors. This is distinct from source retention.
- Decide revocation access policy. Current checked implementation supports explicit `deny_source_access`; if another policy is required, authorize its implementation/review before enrollment. No policy is silently selected.
- Decide eventual encrypted-credential, authorization-audit, source-history, cursor and webhook-receipt retention/purge handling. This milestone implements no purge, deletion job or automatic eviction. Any cleanup deleting historical records needs separate explicit authority.
- Approve operational handling for closed capacity/expiry gates: 32 account records per workspace, 128 authorization generations/states and 512 immutable credential versions per account, 10,000 credential-read and audit records per account, and 4,096 revocation receipts per app. Receipt exhaustion latches authority closed rather than evicting replay evidence.
- Acknowledge that customer disconnect fences only its workspace connection and preserves provider authorization, including potentially shared tokens/grants. Confirm the isolated app/seller has no unrelated integration users before any separately approved provider-wide revocation exercise. Authenticated provider shared-authorization revocation must internally fence all affected seller/application/environment connections without exposing other tenants.

## Proposed later calls and remote changes — approval required

Proposed exact provider operation set (all against the approved sandbox application/seller):

1. Browser `GET https://connect.squareupsandbox.com/oauth2/authorize` with exact redirect, state and five supported read scopes.
2. `POST /oauth2/token` for one consumed authorization code with `short_lived: true`, followed by authenticated `POST /oauth2/token/status`.
3. Authenticated `GET /v2/merchants/me`, `GET /v2/locations`, `GET /v2/locations/main` to establish seller/location/default evidence.
4. Approved read-only Order, Payment, Refund, Catalog and Inventory requests generated by the existing pinned request validators. Before the run, enumerate the concrete synthetic record IDs, locations, bounded page counts, time windows and selected operation/projection cases in the approval record. Do not authorize a wildcard crawler or create real business records.
5. One bounded refresh via `POST /oauth2/token` and subsequent `POST /oauth2/token/status`; replay, expiry and concurrency checks use isolated disposable credentials only.
6. A separately approved grant-level revocation exercise, with exact seller/application/environment impact, operator authority and bounded attempt count specified beforehand, plus authenticated notification delivery to the approved sandbox receiver. The current customer service implements no provider revoke operation or retry. Do not substitute single-access-token revocation for terminating refresh authorization or assume token exclusivity.

Proposed remote changes, each requiring named-target approval: apply reviewed additive migrations to the isolated sandbox database; install finite account/retention configuration and exact LOGIN capabilities; provision or bind only approved sandbox secret/KMS resources; configure exact redirect/notification endpoint and callback query-log suppression; install a reviewed bounded sandbox host/transport. No Production registration, QBO changes, economic authorization, scheduler or public customer navigation is included.

## Acceptance and cleanup to approve

- Fresh authenticated consent → encrypted broker storage → verified discovery → explicit mapping → separate checked enrollment → pending-only atomic ingestion; all foreign actor/session/workspace/entity/seller/location/app/redirect/scope cases deny.
- Confirm actual Square confidential-flow token lifetimes, non-expiring multi-use refresh semantics, exact granted scopes in Sandbox, signed notification payload/retry behavior and seller-wide revocation effects.
- Prove lost acknowledgements, independent-session callback/refresh/disconnect races, private cursor restart and stale commits preserve immutable pending history, cumulative incomplete state and lease/generation/CAS fencing.
- Prove workspace-only disconnect and connection-scoped token failures leave other tenants unchanged, while authenticated shared revocation fences all affected connections. Verify delayed old-grant notifications cannot silently revoke newer consent. Stop on inconclusive identity/authorization scope, privacy leakage, unapproved external actions, expired approval or failed/inconclusive provider cleanup; record the unresolved outcome without claiming successful global revocation.
- Observe callback history, redirects, referrers, third-party assets, browser console, host/edge/application logs and stored rows for synthetic canaries. Require upstream authorization-code query-log suppression before the first real callback.
- Disable sandbox host/enrollment capabilities and fence tasks; through a separately approved grant-level mechanism revoke only the explicitly approved sandbox seller/application consent, and confirm its result and receiver shutdown. Customer disconnect alone does not establish remote cleanup. Revert test-only configuration/memberships using named-target changes. Any secret destruction, synthetic business-record removal or historical purge must be separately itemized and approved; do not infer deletion authority from this worksheet.
- Record exact refs, provider requests, remote mutations, test outcomes and remaining limitations. Passing Sandbox does not activate Square in Production or authorize accounting/reconciliation.

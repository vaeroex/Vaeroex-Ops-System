# Read-only Square Sandbox workspace evidence

The authenticated Vaeroex workspace Settings experience renders `SquareEvidenceCard` only after a successful checked read. It is not an activation of the dormant Square customer routes or registry. The only selected host is `https://square-sandbox.vaeroex.com`, in a separate GCP workspace process during an exclusive bounded evidence window. All Vercel environments (including Production and Preview) are denied. Explicit server-only `SQUARE_EVIDENCE_HOST=gcp-square-sandbox-workspace-v1`, production Node mode, the exact Sandbox database/application URLs, and exact request Host/forwarded-host/HTTPS are required before any RPC. Missing, alternate, comma-separated, IP, localhost, port-suffixed and trailing-dot hosts fail closed. The standardized Forwarded header is rejected rather than interpreted as a competing authority.

The card derives counts from retained canonical facts, never hardcodes the hosted fixture totals. Qualification baseline: 1 Payment, 1 Refund, 3 Orders, 2 Catalog variations, 6 Inventory observations; 2 location-unresolved relationships and 1 conflicting reference. ID matches do not establish financial reconciliation. Historical completeness and current sync health remain explicitly unknown. Observation time is not successful-sync time.

## Authority and privacy

Additive migration 113 provides a narrowly minimized authenticated reader. A SQL-standard, pre-parsed invoker wrapper resolves one private definer function without granting private schema usage. Its elevated read is necessary because raw tables must remain inaccessible; it performs no writes and grants no source, broker, enrollment or interpretation capability. An actual disposable PostgreSQL test verifies that authenticated can call only the checked reader while direct table/schema access remains denied.

It checks verified JWT issuer/subject/session, current session existence/expiry, active membership (owner/admin/manager), user deletion/ban, active entity, exact Sandbox application, current connection/generation, seller, OAuth evidence, enrollment, revocation, retention policy and capacity/configuration fences. Multiple candidate connections fail closed. The selected partition is the distinct resource set already admitted for that connection/generation, not arbitrary latest or a sum of overlapping partitions. Every manifest member passes the existing immutable source/current-version/location validator and matches retained admission and interpretation facts. Expired write approval is not reused as a read credential.

Only bounded counts, uncertainty categories, checkpoint revision/times and per-resource source-version ordinals/times enter the UI. No workspace/entity/connection/seller/location/provider/source IDs, fingerprints, credentials, private cursors, source payloads, amounts or quantities are returned. Full immutable provenance stays private. Errors have one fixed outcome and no logged database/provider payload. No shared cache, mutation button, polling, model import or AI dispatch is added. Unavailable/invalid results hide the card rather than show stale or fabricated counts.

## Delivery and pending hosting

Migration 113 is committed for review only: **not applied remotely**. No configuration, deployment, credential, mapping, enrollment, consent, Production or QBO change is part of this milestone. Existing closed Sandbox gates continue to deny reads. The stopped GCP consent portal is a separate application: this PR targets the authenticated Vaeroex workspace Settings page, not its onboarding HTML. Hosted visibility requires deploying this workspace experience against the isolated Sandbox configuration and separately authorizing application of 113; it cannot appear in the Production workspace by copying Sandbox data or bypassing the database gate.

## Validation

- Native owned local PostgreSQL: full migration chain, checked reader allow/deny, minimized DTO parity, replay/read-only behavior, raw-table denial, preserved QBO schema and economic tables.
- Focused render tests: 13-record baseline, exact uncertainty copy, count/provenance consistency, identifier injection rejection, invalid responses, deployment gate and zero-AI tripwire.
- Synthetic ephemeral browser: desktop/mobile provenance disclosure, read-only controls, bounded layout and zero network requests. No hosted identity is involved.
- Existing ingestion/canonical tests retain their zero-AI guard; CI's observations suite includes the new render and browser tests.

## Selected hosting boundary and pending qualification

Repository inspection: `services/square-sandbox-callback/src/server.ts` owns its own TLS listener and credential-aware portal, while `app/app/settings/page.tsx` is a Next workspace page. Do not add Next routes or credentials to that portal. Use the existing hostname, regional IP, certificate and bounded VM only in an **exclusive** evidence window: consent daemon stopped, separate unprivileged Next process, no broker/Secret Manager/KMS authority, no database password or Square credential. This avoids new DNS/resources and preserves the consent implementation unchanged. Do not run both services concurrently or claim this process is already installed.

The application header check is defense in depth, not proof of network origin. Before deployment the dedicated TLS reverse proxy must validate exact TLS SNI and raw HTTP Host (both `square-sandbox.vaeroex.com`), reject missing/duplicate/alternate Host and direct-IP SNI, reject client-supplied Forwarded and X-Forwarded-* headers, and generate the three checked headers itself. It must connect to Next only over a private Unix socket or loopback interface inaccessible from ingress. Do not merely rewrite arbitrary incoming Host to the approved value. No wildcard/default virtual host, alternate certificate name, direct Next port or public origin bypass is permitted. Keep the existing single-operator IP restriction and deadline cleanup. A caller resolving the approved DNS name to its real IP while retaining the approved SNI/Host is ordinary HTTPS, not an alternate host.

Finite hosted plan (not executed by this correction):

1. Review the exact TLS proxy/service configuration and locally test SNI/Host mismatch, duplicate/spoofed forwarding headers, and direct upstream isolation before installing anything. Confirm the workspace build uses only isolated Supabase publishable configuration and no privileged platform/AI/provider credentials. Restrict ingress to sign-in, sign-out, Settings and their required static assets; deny other mutation routes/actions. Verify the existing workspace session/subscription prerequisites without modifying membership or onboarding.
2. In a separately authorized window, apply only 113 against the pinned Sandbox ledger, start the isolated evidence host with bounded memory/time, retain all broker roles NOLOGIN and provider/enrollment gates closed. Permit only the checked evidence read through its existing current-authority fence. Record and restore any temporary read configuration.
3. Privately authenticate the existing Sandbox user. Verify the actual Settings card and network response: 13 observations, source-version provenance/checkpoint, 2 unresolved location relationships, 1 conflict, unknown history and blocked economics. Verify unrelated-workspace denial with an independently authorized test session, not an administrator-emulated UI. Inspect only minimized outputs.
4. Run hosted rejected-host/TLS cases, verify no payloads/private cursors/identifiers or AI calls, then stop the workspace service/VM and restore closed ingress/read configuration. Do not modify consent, generation, mapping, enrollment or QBO.

The application gate is covered by synthetic tests; TLS proxy installation and hosted identity/UI assertions remain pending and must not be described as already proven. No migration, merge or deployment is authorized by this correction. Next's supported async request headers and self-hosting guidance: https://nextjs.org/docs/app/api-reference/functions/headers and https://nextjs.org/docs/app/guides/self-hosting.

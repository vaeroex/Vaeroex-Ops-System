# Upstream revision and direct runtime dependencies are pinned. Install through
# install-worker-dependencies.sh so NeMo Retriever receives deterministic build metadata.
cryptography==50.0.0
httpx==0.28.1
Pillow==12.2.0
ray[data,serve]==2.55.1
vercel==0.8.1
nemo-retriever[nemotron-parse] @ git+https://github.com/NVIDIA/NeMo-Retriever.git@52886112cafab4c4bca1cda0d4f588785adfe4d3#subdirectory=nemo_retriever

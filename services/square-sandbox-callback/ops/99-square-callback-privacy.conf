# Proposed /etc/sysctl.d/99-square-callback-privacy.conf, guest only.
# A pipe core handler can bypass a process's RLIMIT_CORE. Discard at the source.
kernel.core_pattern=|/bin/false
fs.suid_dumpable=0

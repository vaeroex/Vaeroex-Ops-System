import theme from "../../../tailwind.config";
import type { SquareWorkspaceEvidence } from "@/lib/integrations/providers/square/workspace-evidence";
import type { SquareWorkspaceOperational } from "@/lib/integrations/providers/square/workspace-operational";

// Share only the inert Production design palette. AppShell/AppNavigation import
// actions, live workspace data and client code and must never enter this host.
const palette = theme.theme!.extend!.colors as { vaeroex: { navy: string; blue: string; accent: string } };
export const escape = (s: unknown) => String(s).replace(/[&<>"']/g, c => ({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]!));
const styles = `
:root{color-scheme:light;font-family:Inter,ui-sans-serif,system-ui,sans-serif;color:${palette.vaeroex.navy};background:#f8fafc}*{box-sizing:border-box;min-width:0}body{margin:0}a{color:inherit}button,input,select{font:inherit}button,a,input,select{min-height:44px}button{cursor:pointer;border:1px solid #cbd5e1;border-radius:8px;padding:10px 14px;background:white;color:inherit;font-weight:600}button:hover{background:#eaf4ff}a:focus-visible,button:focus-visible,input:focus-visible,select:focus-visible,summary:focus-visible{outline:3px solid ${palette.vaeroex.accent};outline-offset:3px}.skip{position:absolute;top:-100px}.skip:focus{top:8px;left:8px;background:white;z-index:10;padding:12px}.sidebar{position:fixed;inset:0 auto 0 0;width:256px;background:${palette.vaeroex.navy};color:white;padding:24px 16px;display:flex;flex-direction:column;gap:24px}.brand{font-size:24px;font-weight:750;text-decoration:none;letter-spacing:-1px}.badge{display:inline-block;border:1px solid #7dd3fc;border-radius:999px;padding:5px 10px;font-size:12px;font-weight:700;letter-spacing:.05em;background:#e0f2fe;color:#0b1f4d}.sidebar nav{display:grid;gap:8px}.sidebar nav a{display:flex;align-items:center;padding:10px 12px;border-radius:8px;text-decoration:none}.sidebar nav a:hover{background:#164174}.sidebar nav .active{background:${palette.vaeroex.blue}}.sidebar p{color:#cbd5e1;font-size:13px;line-height:1.6}.frame{margin-left:256px}header{position:sticky;top:0;z-index:2;background:${palette.vaeroex.navy};color:white;padding:20px 28px;display:flex;align-items:center;justify-content:space-between;gap:16px}header h1{font-size:21px;margin:4px 0}header p{font-size:12px;color:#cbd5e1;margin:0}.main{max-width:1480px;margin:auto;padding:28px;display:grid;gap:24px}.panel{background:white;border:1px solid #dbe2ec;border-radius:12px;padding:24px;box-shadow:0 16px 42px #0b1f4d0a}.eyebrow{text-transform:uppercase;letter-spacing:.14em;font-size:11px;font-weight:750;color:#475569}h2{font-size:26px;margin:8px 0 12px}h3{font-size:18px;margin:0 0 12px}p{line-height:1.65}.muted{color:#475569;font-size:14px}.toolbar{display:flex;align-items:end;justify-content:space-between;gap:16px;flex-wrap:wrap}.toolbar form{display:flex;align-items:end;gap:10px;flex-wrap:wrap}label{display:grid;gap:8px;font-size:13px;font-weight:600}select,input{border:1px solid #94a3b8;border-radius:8px;background:white;color:#0b1f4d;padding:10px;max-width:100%}.kpis{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:12px;margin-top:20px}.kpi{border:1px solid #dbe2ec;border-radius:10px;padding:18px;background:#f8fafc}.kpi strong{display:block;font-size:34px;font-variant-numeric:tabular-nums;margin:10px 0}.kpi span{font-size:13px;font-weight:600}.kpi small{font-size:12px;color:#475569}.grid{display:grid;grid-template-columns:1fr 1fr;gap:20px}dl{margin:0}dt{font-size:12px;color:#475569;margin-top:14px}dd{margin:5px 0;font-size:14px;overflow-wrap:anywhere}.notice{border-left:4px solid #d97706;background:#fffbeb}.notice p{margin-bottom:0}.scroll{overflow-x:auto}table{min-width:620px;border-collapse:collapse;width:100%;font-size:13px}caption{text-align:left;font-weight:600;padding:12px 0}th,td{text-align:left;border-bottom:1px solid #e2e8f0;padding:12px 8px;vertical-align:top}th{background:#f8fafc}td{overflow-wrap:anywhere}summary{cursor:pointer;font-weight:650;padding:6px 0}section,article{scroll-margin-top:110px}.signin{max-width:500px;margin:48px auto}.signin form{display:grid;gap:18px}.primary{background:${palette.vaeroex.blue};color:white}.primary:hover{background:#1d4ed8}footer{font-size:12px;color:#475569;line-height:1.7;padding:0 4px 24px}h1,h2,h3{overflow-wrap:anywhere}@media(max-width:1000px){.kpis{grid-template-columns:repeat(3,minmax(0,1fr))}}@media(max-width:760px){.sidebar{position:static;width:auto;padding:16px;gap:12px}.sidebar nav{display:flex;flex-wrap:wrap;gap:4px}.sidebar p{display:none}.frame{margin:0}header{position:sticky;padding:16px}.main{padding:16px;gap:16px}.panel{padding:18px}.grid{grid-template-columns:1fr}.kpis{grid-template-columns:repeat(2,minmax(0,1fr))}.signin{margin:12px auto}.toolbar form{width:100%}.toolbar label{width:100%}header h1{font-size:18px}}@media(prefers-reduced-motion:no-preference){html{scroll-behavior:smooth}}`;

export function document(body: string) {
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>Vaeroex Executive Intelligence Sandbox</title><style>${styles}</style></head><body><a class="skip" href="#content">Skip to content</a><aside class="sidebar"><a class="brand" href="/evidence">Vaeroex</a><div><span class="badge">Sandbox</span><p>Executive Intelligence<br>Private, isolated workspace</p></div><nav aria-label="Sandbox workspace navigation"><a href="/evidence#overview">Overview</a><a href="/activity">Activity explorer</a><a class="active" href="/evidence#square">Square integration</a><a href="/evidence#provenance">Evidence</a><a href="/evidence#limitations">Limitations</a></nav><p>Production and QBO are not connected here. No economic contributions or recommendations.</p></aside><div class="frame"><header><div><p>PRIVATE WORKSPACE · SANDBOX ONLY</p><h1>Vaeroex Executive Intelligence Sandbox</h1></div><span class="badge">Sandbox</span></header><main class="main" id="content">${body}<footer>Vaeroex Executive Intelligence Sandbox · Read-only, non-economic evidence.<br>Available only during an operator-started bounded session. Closing this browser does not extend the session or start a sync.</footer></main></div></body></html>`;
}

export function controls(workspaces: readonly string[], selected: string | undefined) {
  return `<div class="toolbar"><form method="post" action="/workspace"><label>Workspace<select name="workspace">${workspaces.map(name=>`<option${name===selected?' selected':''}>${escape(name)}</option>`).join('')}</select></label><button>View workspace</button></form><form method="post" action="/signout"><button>Sign out</button></form></div>`;
}

export function card(e: SquareWorkspaceEvidence) {
  const labels = {payment:"Payment",refund:"Refund",order:"Orders",catalog:"Catalog variations",inventory:"Inventory observations"} as const;
  // These are the checked admitted-observation counts, not transaction volume,
  // sales, present stock, or counts inferred from a partial provider response.
  const metrics = Object.entries(labels).map(([kind,label])=>`<article class="kpi" aria-label="${label}: ${e.counts[kind as keyof typeof labels]}"><span>${label}</span><strong>${e.counts[kind as keyof typeof labels]}</strong><small>Admitted observations</small></article>`).join('');
  return `<section id="square" class="panel"><span class="eyebrow">Integrations / Square Sandbox</span><h3>Square Sandbox evidence</h3><span class="badge">Verified · Non-economic · Read-only</span><p class="muted">Verified, non-economic provider observations · Read-only. These counts describe admitted evidence, not business performance.</p><div id="activity"><h3>Descriptive activity</h3><div class="kpis">${metrics}</div></div></section><div class="grid"><section class="panel"><h3>Checkpoint &amp; freshness</h3><dl><dt>Interpretation checkpoint</dt><dd>${e.checkpointRevision} · ${escape(e.interpretedAt)}</dd><dt>Last verified observation</dt><dd>${escape(e.lastObservedAt)}</dd><dt>Authority checked at</dt><dd>${escape(e.checkedAt)}</dd><dt>Current sync health</dt><dd>Unknown — page retrieval is not a sync.</dd></dl></section><section class="panel notice" id="limitations"><h3>Evidence limitations</h3><p>${e.relationships.unresolvedLocation} unresolved location relationships; ${e.relationships.conflict} reference conflict.</p><p>${e.relationships.otherUncertain} other uncertain relationships. Historical completeness: unknown.</p><p class="muted">An ID match alone does not prove a relationship or accounting meaning. No revenue, profit, netting, inventory valuation, stock calculation, or complete-history claim. Economic contributions remain blocked.</p></section></div><section class="panel" id="provenance"><h3>Source &amp; provenance</h3><p class="muted">Source: Square Sandbox · Immutable source provenance · ${escape(e.policy)}</p><p class="muted">Catalog is seller-scoped with location applicability. Payments, Refunds, Orders and Inventory remain distinct observations.</p><details><summary>Inspect ${e.provenance.length} admitted source versions</summary><div class="scroll" tabindex="0" role="region" aria-label="Source provenance"><table><caption>Sanitized source versions — provider identifiers intentionally omitted</caption><thead><tr><th scope="col">Resource</th><th scope="col">Version</th><th scope="col">Observed (provider timestamp)</th><th scope="col">Authority scope</th></tr></thead><tbody>${e.provenance.map(p=>`<tr><td>${escape(p.kind)}</td><td>${p.sourceVersion}</td><td>${escape(p.observedAt)}</td><td>${p.scope==='seller_with_location_applicability'?'Seller; location applicability':'Mapped location or explicitly unresolved'}</td></tr>`).join('')}</tbody></table></div></details></section>`;
}

const money=(value:{amountMinor:string;currency:string}|null)=>value?`${escape(value.currency)} ${escape(value.amountMinor)} minor units`:"Unavailable";
export function operational(view:SquareWorkspaceOperational,filter:{kind:string;status:string;page:number}){
  const c=view.calculation;
  const summary=(label:string,value:typeof c.payment)=>`<article class="kpi"><span>${label}</span><strong>${value.count}</strong><small>${money(value.total)} · average ${value.exactAverageMinor===null?escape(value.averageState):escape(value.exactAverageMinor)+' minor units'}</small></article>`;
  const rows=view.page.rows.map(row=>`<tr><td>${escape(row.evidenceRef)}</td><td>${escape(row.kind)}</td><td>${escape(row.status)}</td><td>${row.occurredAt?escape(row.occurredAt):'Unknown'}</td><td>${row.label?escape(row.label):'Not retained'}</td><td>${money(row.amount)}</td><td>${row.quantity?escape(row.quantity):'—'}${row.unitState==='unverified'?' (unit unverified)':''}</td><td>${escape(row.relationship)}</td></tr>`).join('');
  const groups=c.groups.map(group=>`<tr><td>${escape(group.kind)}</td><td>${escape(group.locationGroup)}</td><td>${group.currency?escape(group.currency):'Unknown'}</td><td>${group.summary.count}</td><td>${money(group.summary.total)}</td><td>${group.summary.exactAverageMinor??escape(group.summary.averageState)}</td></tr>`).join('');
  return `<section class="panel"><span class="eyebrow">Square Sandbox · deterministic operations</span><h2>Operational activity</h2><p class="muted">Verified non-economic observations only. Amounts are provider activity in exact currency minor units—not revenue, settlement, profit or net sales.</p><div class="kpis">${summary('Completed payments',c.payment)}${summary('Completed refunds',c.refund)}${summary('Observed orders',c.order)}</div><details><summary>Exact currency and location groups</summary><div class="scroll"><table><thead><tr><th>Resource</th><th>Opaque location group</th><th>Currency</th><th>Count</th><th>Total</th><th>Exact average/state</th></tr></thead><tbody>${groups}</tbody></table></div></details></section>
  <section class="panel"><h3>Filters</h3><form method="post" action="/activity-filter" class="toolbar"><label>Resource<select name="kind">${['','payment','refund','order','catalog','inventory'].map(k=>`<option value="${k}"${filter.kind===k?' selected':''}>${k||'All resources'}</option>`).join('')}</select></label><label>Status<input name="status" maxlength="80" value="${escape(filter.status)}"></label><label>Page<input name="page" type="number" min="1" max="40" value="${view.page.page}"></label><button>Apply</button></form><div class="scroll" role="region" aria-label="Square activity"><table><caption>${view.page.total} sanitized observations · page ${view.page.page} of ${view.page.pages}</caption><thead><tr><th>Evidence</th><th>Resource</th><th>Status</th><th>Observed</th><th>Label</th><th>Amount</th><th>Quantity</th><th>Relationship</th></tr></thead><tbody>${rows}</tbody></table></div></section>
  <div class="grid"><section class="panel"><h3>Deterministic status mix</h3><dl>${Object.entries(c.statusMix).flatMap(([kind,mix])=>Object.entries(mix).map(([s,n])=>`<dt>${escape(kind)} · ${escape(s)}</dt><dd>${n} observed records</dd>`)).join('')}</dl><p class="muted">Refund count ratio: ${c.refundRate.numerator}/${c.refundRate.denominator} completed observations. This is not a financial refund rate.</p></section><section class="panel notice"><h3>Required unknowns</h3><p>Catalog active count: unknown (${escape(c.catalog.activeState)}).</p><p>Inventory movement: unknown (${escape(c.inventory.movementState)}).</p><p>Fulfillment activity: unknown (${escape(c.fulfillment.state)}).</p><p>Historical completeness remains unknown; trends are not claimed.</p></section></div>
  <section class="panel"><h3>Deterministic insights</h3>${c.insights.map(i=>`<article><strong>${escape(i.state)}</strong><p>${escape(i.text)}</p><p class="muted">Rule: ${escape(i.rule)}${i.blockedReason?` · Blocked: ${escape(i.blockedReason)}`:''} · Evidence references: ${i.evidenceRefs.length}</p></article>`).join('')}<p class="muted">AI dispatch: disabled. Duplicate delivery, replay, rendering and ordinary sync cannot invoke a model.</p></section>`;
}
